<UserControl x:Class="CL.WinApp.userControl.CardItem"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CL.WinApp.userControl"
             mc:Ignorable="d" 
             d:DesignHeight="400" d:DesignWidth="400"
             x:Name="root">
    <ToggleButton IsChecked="{Binding Card.IsReturned}" x:Name="buttonCard" Click="buttonCard_Click">
        <TextBlock Text="{Binding TextToPrint,ElementName=root}"/>
    </ToggleButton>

</UserControl>
